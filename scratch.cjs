const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('https://forum.babylonjs.com/search?q=quest%203%20controller'); await page.waitForSelector('.topic-title'); const results = await page.$eval('.topic-title', els => els.map(e => e.href)); console.log(results.slice(0, 3)); for (const url of results.slice(0, 2)) { await page.goto(url); const text = await page.$eval('.cooked', e => e.innerText); console.log('--- ' + url + ' ---'); console.log(text.substring(0, 500)); } await browser.close(); })().catch(console.error);
